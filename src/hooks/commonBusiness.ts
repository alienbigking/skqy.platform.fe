/*
 * 这里封装通用的业务逻辑代码，禁止编写与业务逻辑无关的hooks
 * */
interface IManagementServiceInfoProps {}

interface IProfileInfo {}

// 获取管理服务
const useManagementServiceInfo = (props?: IManagementServiceInfoProps) => {}

export { useManagementServiceInfo }

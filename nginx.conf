user  nginx;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    client_max_body_size 10g;
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    gzip_static on;
    gzip_http_version 1.0;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript;
    gzip_vary on;

    include /etc/nginx/conf.d/*.conf;


    server {
        listen 80;
        server_name localhost test.innomedi.cn;

        root /usr/share/nginx/html;
        index index.html;

        # 前端 SPA 路由支持
        location / {
            try_files $uri /index.html;
        }
    }

    # =====================主Nginx已经代理相关请求，server 块可以注释掉，若未代理则可以开启 =====================
#     server {
#         listen 80;
#         server_name localhost 127.0.0.1 test.innomedi.cn;   # 当 Nginx 收到 HTTP 请求时，会根据请求头中的 Host 值去匹配合适的 server 块
#
#         root /usr/share/nginx/html;
#         index index.html;
#
#         # 前端 SPA 路由支持
#         location / {
#             try_files $uri /index.html;
#         }
#
#         # 代理接口请求到后端
#         location /yxpt/ {
#             proxy_pass https://test.innomedi.cn/;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Host $server_name;
#             proxy_redirect off;
#         }
#     }
#
#     server {
#         listen 80;
#         server_name ws-test.innomedi.cn;   # 当 Nginx 收到 HTTP 请求时，会根据请求头中的 Host 值去匹配合适的 server 块
#
#         # 代理 WebSocket
#         location /ws {
#             proxy_pass http://websocket;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "Upgrade";
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Host $server_name;
#             proxy_redirect off;
#         }
#     }
}
